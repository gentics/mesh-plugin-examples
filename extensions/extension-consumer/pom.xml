<project xmlns="http://maven.apache.org/POM/4.0.0"
	xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
	xsi:schemaLocation="http://maven.apache.org/POM/4.0.0 http://maven.apache.org/xsd/maven-4.0.0.xsd">
	<modelVersion>4.0.0</modelVersion>

	<artifactId>plugin-extension-consumer</artifactId>
	<parent>
		<groupId>com.gentics.mesh.plugin</groupId>
		<artifactId>plugin-extension-example</artifactId>
		<version>0.0.1-SNAPSHOT</version>
	</parent>

	<properties>
		<plugin.id>extension-consumer</plugin.id>
		<plugin.name>The extension consumer</plugin.name>
		<plugin.description>A plugin which consumes an extension provided by
			another plugin</plugin.description>
		<plugin.class>com.gentics.mesh.plugin.ExtensionConsumerPlugin</plugin.class>
	</properties>

	<dependencies>
		<!-- Commonly shared plugin api for the extension -->
		<dependency>
			<groupId>com.gentics.mesh.plugin</groupId>
			<artifactId>plugin-extension-api</artifactId>
			<version>${project.version}</version>
		</dependency>

		<!-- Gentics Mesh Plugin API -->
		<dependency>
			<groupId>com.gentics.mesh</groupId>
			<artifactId>mesh-plugin-api</artifactId>
			<scope>provided</scope>
		</dependency>

		<!-- Test dependencies -->
		<dependency>
			<groupId>junit</groupId>
			<artifactId>junit</artifactId>
			<scope>test</scope>
		</dependency>
		<dependency>
			<groupId>com.gentics.mesh</groupId>
			<artifactId>mesh-test-common</artifactId>
			<scope>test</scope>
		</dependency>
		<dependency>
			<groupId>com.gentics.mesh</groupId>
			<artifactId>mesh-core</artifactId>
			<scope>test</scope>
		</dependency>
	</dependencies>

	<build>
		<plugins>
			<plugin>
				<groupId>org.apache.maven.plugins</groupId>
				<artifactId>maven-shade-plugin</artifactId>
				<executions>
					<execution>
						<phase>package</phase>
						<goals>
							<goal>shade</goal>
						</goals>
						<configuration>
							<transformers>
								<transformer
									implementation="org.apache.maven.plugins.shade.resource.ManifestResourceTransformer">
									<manifestEntries>
										<Plugin-Id>${plugin.id}</Plugin-Id>
										<Plugin-Version>${plugin.version}</Plugin-Version>
										<Plugin-Author>${plugin.author}</Plugin-Author>
										<Plugin-Class>${plugin.class}</Plugin-Class>
										<Plugin-Description>${plugin.description}</Plugin-Description>
										<Plugin-License>${plugin.license}</Plugin-License>
										<Plugin-Inception>${plugin.inception}</Plugin-Inception>
									</manifestEntries>
								</transformer>
							</transformers>
						</configuration>
					</execution>
				</executions>
			</plugin>
		</plugins>
	</build>

</project>